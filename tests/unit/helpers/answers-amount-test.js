import { answersAmount } from 'q-and-a/helpers/answers-amount';
import { module, test } from 'qunit';

module('Unit | Helper | answers amount');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answersAmount([42]);
  assert.ok(result);
});
